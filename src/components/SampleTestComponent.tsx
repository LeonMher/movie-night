import styled from 'styled-components';

const StyledHeading = styled.h1`
  color: white;
  font-size: 90px;
`;

const SampleTestComponent = () => {
  return (
    <div>
      <StyledHeading>Movie Night</StyledHeading>
    </div>
  );
};

export default SampleTestComponent;
